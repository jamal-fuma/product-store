<!-- hackily replace the form url with javascript -->
  <% form_tag product_categories_path(product), :method => :post, :id=>'assign-product-category-form', :class => 'button' do %>
      <%= select :category,:id, Category.to_list %>
      <%= submit_tag 'Assign category' %>
  <% end %>
