<!-- hackily replace the form url with javascript -->
  <% form_tag product_suppliers_path(product), :method => :post, :id=>'assign-product-supplier-form', :class => 'button' do %>
      <%= select :supplier,:id, Supplier.drop_down_list %>
      <%= submit_tag 'Assign supplier' %>
  <% end %>
