<!-- hackily replace the form url with javascript -->
  <% form_tag image_categories_path(image), :method => :post, :id=>'assign-image-category-form', :class=>'button' do %>
      <%= select :category,:id, Category.drop_down_list %>
      <%= submit_tag 'Assign category' %>
  <% end %>
