<!-- hackily replace the form url with javascript -->
  <% form_tag category_images_path(category), :method => :post, :id=>'assign-category-image-form', :class => 'button' do %>
      <%= select :image,:id, Image.to_list %>
      <%= submit_tag 'Assign image' %>
  <% end %>
