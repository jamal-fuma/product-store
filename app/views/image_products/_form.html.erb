<!-- hackily replace the form url with javascript -->
  <% form_tag image_products_path(image), :method => :post, :id=>'assign-image-product-form', :class=>'button' do %>
      <%= select :product,:id, Product.drop_down_list %>
      <%= submit_tag 'Assign product' %>
  <% end %>
